<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.opaku.id">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Opaku">
        <activity
            android:name=".ui.register.RegisterActivity"
            android:theme="@style/Theme.Opaku.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/Theme.Opaku.NoActionBar" />
        <activity
            android:name=".ui.favoriteproduct.FavoriteProductActivity"
            android:theme="@style/Theme.Opaku.NoActionBar" />
        <activity
            android:name=".ui.offerscreen.OfferScreenActivity"
            android:theme="@style/Theme.Opaku.NoActionBar" />
        <activity
            android:name=".ui.detailproduct.DetailProductActivity"
            android:theme="@style/Theme.Opaku.NoActionBar" />
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:theme="@style/Theme.Opaku.NoActionBar" />
        <activity
            android:name=".ui.splashscreen.SplashScreenActivity"
            android:theme="@style/Theme.Opaku.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>